import { styled } from "styled-components";

export const MainContainer = styled.main`
    width: 46.875rem;
    margin: 5px auto;

    display: flex;
    justify-content: center;
    gap: 10px;
    flex-wrap: wrap;
`;